// When background submitted, the button receives the disabled flag
// After successful execution the whole row can be removed from the dom
$('#reminders_table button:disabled').closest('tr').remove();