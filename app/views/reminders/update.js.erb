// When background submitted, the button receives the disabled flag
// After successful execution it gets removed from the dom
document.querySelectorAll('#reminders_table button:disabled').forEach(function(button) { button.remove(); });
