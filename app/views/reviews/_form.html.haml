= boot_form_for [@review.kid, @review], :html => { :id => 'content_form' } do |form|
  = date_picker @review, form, :held_at
  = form.input :attendee
  = form.input :reason
  = form.input :kind
  = form.input :content
  = form.input :outcome
  = form.input :note

  = form.buttons
