- content_for :sidebar do
  - if current_user.is_a?(Admin)
    %li
      %h4 Filterkriterium f&uuml;r Mentoren
      = form_for :mentor, :url => { :action => 'index' }, :html => { :method => :get, :class => 'filter' } do |f|
        .form-group
          = f.select :term, term_collection, { :include_blank => 'Startzeit' }, class: 'form-control'
        .form-group
          = f.select :primary_kids_school_id, school_collection, { :include_blank => Mentor.human_attribute_name(:primary_kids_school) }, class: 'form-control'
        .form-group
          = f.select :primary_kids_meeting_day, wday_collection, { :include_blank => Mentor.human_attribute_name(:primary_kids_meeting_day) }, class: 'form-control'
        .form-group
          = f.select :transport, transport_collection, { :include_blank => 'ÖV' }, class: 'form-control'
        .checkbox
          %label
            = f.check_box :ects
            %p ECTS
        .checkbox
          %label
            = f.check_box :inactive
            %p Inaktive
        .form-group
          = f.submit 'Filter anwenden', class: 'btn btn-xs btn-success'
          = link_to 'Filter aufheben', collection_path, class: 'btn btn-xs btn-danger'

= contextual_links
= boot_page_title do
  = collection.count if current_user.is_a?(Admin)
  Mentoren

= render '/users/list'
