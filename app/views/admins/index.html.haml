- content_for :sidebar do
  -if current_user.is_a?(Admin)
    = form_for :admin, :url => { :action => 'index' }, :html => { :method => :get, :class => 'filter' } do |f|
      .checkbox
        %label
          = f.check_box :inactive
          %p
            %b Inaktive

      .form-group
        = f.button (icon('funnel') + ' Filter anwenden'), class: 'btn btn-sm btn-primary'
        = link_to (icon('x-lg') + ' Filter aufheben'), admins_path, class: 'btn btn-sm btn-secondary'

= boot_page_title do
  = @admins.count if current_user.is_a?(Admin)
  Administratoren

= render 'users/list', collection: @admins
