= render layout: 'index_header' do
  = new_button(Principal)

- if current_user.is_a?(Admin)
  = render layout: 'filter_bar', locals: { record: @principal } do
    = render 'inactive_filter', record: @principal

= render 'users/list', collection: @principals
