.row
  .col-md-8
    .card.mb-4
      .card-header
        %h4.card-title.mb-0 Persönliche Angaben
      .card-body
        = show_for @teacher do |show|
          = show.attribute :name
          = show.attribute :prename
          = show.attribute(:email) { mail_to(@teacher.email) { icon('envelope') + ' ' + @teacher.email } }
          = show.attribute :school do
            = can_link_to(@teacher.school)
          = show.attribute :phone
          = show.association :kids do |k|
            %li= can_link_to(k)
          = show.association :secondary_kids do |k|
            %li= can_link_to(k)
          = show.attribute :receive_journals
  .col-md-4
    = profile_photo(@teacher)

-if current_user.is_a?(Admin)
  .card.mb-4
    .card-header
      %h4.card-title.mb-0 Projektinterne Informationen
    .card-body
      = show_for @teacher do |show|
        = show.attribute :todo
        = show.attribute :note
        = show.attribute :inactive
        - if @teacher.inactive?
          = show.attribute :inactive_at

  = render 'relation_logs/collection', resource: @teacher
